import { Game } from "../typings";

export const fetchGames = async() => {
    // const res = await fetch(`${process.env.NEXT_PUBLIC_BASE_URL}/api/getJeux`);
    // const data = await res.json();
    // const jeux: Game[] = data.games;

    // console.log("fetching", jeux);

    return "jeux";
}